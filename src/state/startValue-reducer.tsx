
type InitialStateType = typeof InitialState
type ActionType = {}


const InitialState = {
    startValue: 0
}



export const startValueReducer = (state: InitialStateType, action: ActionType): InitialStateType => {
    switch (action.type) {
        case:
        default:
            return state
    }

}